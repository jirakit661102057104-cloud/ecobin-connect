# Legacy Vite mockup (ไม่ใช้รันจริง)

โฟลเดอร์นี้เก็บ mockup เดิมจาก Google AI Studio (Vite + React)  
ก่อนย้ายไป **Next.js** ที่ `frontend/`

อย่ารัน `npm install` / `npm run dev` จากที่นี่สำหรับงานจริง  
ใช้ `frontend/` + `backend/` + `run.bat` ที่ root แทน
