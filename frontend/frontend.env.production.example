# Vercel: leave NEXT_PUBLIC_API_URL empty so the browser calls same-origin /api
# Set API_PROXY_TARGET in Vercel env to the public Go API URL (not localhost)
FRONTEND_PORT=3000
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_DEMO_LOGIN=false
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
